use crate::gui::views::default::DefaultViewMessage;
use iced::Element;

pub struct ActionPanel {}

impl ActionPanel {
    pub fn view(&mut self) -> Element<DefaultViewMessage> {
        use iced::{Text, Button}
    }
}
